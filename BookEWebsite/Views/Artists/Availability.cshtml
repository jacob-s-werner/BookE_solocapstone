@model BookEWebsite.Models.ArtistAvailability

@{
    ViewData["Title"] = "Availabilty";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>AvailabiltyDetails</h1>

<h4>Add Availability</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Availability">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ArtistId" />
            <div class="avail-starttime">
                <span class="form-group">
                    <label>Start Time </label>
                    <select asp-for="StartTime.Hour" class="form-control" asp-items="ViewBag.Hours"></select>
                </span>
                <span class="form-group">
                    <label> : </label>
                    <select asp-for="StartTime.Minute" class="form-control" asp-items="ViewBag.Minutes"></select>
                </span>
                <span class="form-group">
                    <select asp-for="StartTimeOfDay" class="form-control" asp-items="ViewBag.TimeOfDay"></select>
                </span>
            </div>
            <div class="avail-endtime">
                <span class="form-group">
                    <label>Start Time </label>
                    <select asp-for="EndTime.Hour" class="form-control" asp-items="ViewBag.Hours"></select>
                </span>
                <span class="form-group">
                    <label> : </label>
                    <select asp-for="EndTime.Minute" class="form-control" asp-items="ViewBag.Minutes"></select>
                </span>
                <span class="form-group">
                    <select asp-for="EndTimeOfDay" class="form-control" asp-items="ViewBag.TimeOfDay"></select>
                </span>
            </div>
            <div class="form-group">
                <label asp-for="DayOfWeek" class="control-label"></label>
                <select asp-for="DayOfWeek" class="form-control" asp-items="ViewBag.DaysOfWeek"></select>
                <span asp-validation-for="DayOfWeek" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="Recurring" /> @Html.DisplayNameFor(model => model.Recurring)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<h1>Availability List</h1>
<div class="row">
    <hr />
    @if (Model.AAvailabilitiesList.Count > 0)
    {
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.StartTime)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.EndTime)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DayOfWeek)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Recurring)
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.AAvailabilitiesList)
                {
                    <tr>
                        <td>
                            @item.StartTime.ToShortTimeString()
                        </td>
                        <td>
                            @item.EndTime.ToShortTimeString()
                        </td>
                        <td>
                            @item.DayOfWeek
                        </td>
                        <td>
                            @item.Recurring
                        </td>
                        <td>
                            <a asp-action="AvailabilityDelete" asp-route-id="@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <h4>No Availabilities currently scheduled...</h4>
    }
</div>
<div>
    <a asp-action="Index">Back to HomePage</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
